import{useState}from'react';import{Link,useNavigate}from'react-router-dom';import{request,save}from'./auth';import{Shell,Field,Msg,Btn}from'./Login';export default function Signup(){const[f,setF]=useState({name:'',email:'',password:''}),[error,setError]=useState(''),[busy,setBusy]=useState(false),nav=useNavigate();async function submit(e){e.preventDefault();setBusy(true);setError('');try{const d=await request('signup',f);save(d);nav('/')}catch(x){setError(x.message)}finally{setBusy(false)}}return <Shell title="Create your account" subtitle="Start your AI-powered career journey"><form className="space-y-5" onSubmit={submit}>{error&&<Msg>{error}</Msg>}<Field label="Full name" value={f.name} onChange={name=>setF({...f,name})}/><Field label="Email address" type="email" value={f.email} onChange={email=>setF({...f,email})}/><Field label="Password (8+ characters)" type="password" value={f.password} onChange={password=>setF({...f,password})}/><Btn busy={busy}>Create account</Btn></form><p className="mt-6 text-center text-sm">Already registered? <Link className="text-primary font-bold" to="/login">Login</Link></p></Shell>}
