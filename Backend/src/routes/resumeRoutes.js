const r=require('express').Router(),a=require('../middleware/asyncHandler'),c=require('../controllers/resumeController'),{uploadResume}=require('../middleware/resumeUpload'),{requireAuth}=require('../middleware/auth'),{aiRateLimit}=require('../middleware/aiRateLimit');r.use(requireAuth);r.get('/',a(c.get));r.post('/upload',uploadResume,a(c.upload));r.post('/:id/extract',a(c.extract));r.post('/:id/parse',aiRateLimit,a(c.parse));r.delete('/:id',a(c.remove));module.exports=r;
