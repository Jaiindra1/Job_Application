const r=require('express').Router(),a=require('../middleware/asyncHandler'),c=require('../controllers/dashboardController'),{requireAuth}=require('../middleware/auth'),{aiRateLimit}=require('../middleware/aiRateLimit');r.use(requireAuth);r.get('/summary',a(c.summary));r.get('/matches',a(c.matches));r.get('/recent-applications',a(c.recent));r.get('/insights',aiRateLimit,a(c.insights));r.get('/skills',aiRateLimit,a(c.skills));module.exports=r;
