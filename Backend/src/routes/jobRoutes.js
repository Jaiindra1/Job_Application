const r=require('express').Router(),a=require('../middleware/asyncHandler'),c=require('../controllers/jobController'),adzuna=require('../controllers/adzunaController'),{requireAuth}=require('../middleware/auth'),{aiRateLimit}=require('../middleware/aiRateLimit');const recommendedAuth=(req,res,next)=>req.query.recommended==='true'?requireAuth(req,res,next):next();r.post('/import/adzuna',requireAuth,a(adzuna.importJobs));r.post('/:id/match',requireAuth,aiRateLimit,a(c.match));r.post('/:id/cover-letter',requireAuth,aiRateLimit,a(c.coverLetter));r.post('/:id/cover-letter/drafts',requireAuth,a(c.saveCoverLetterDraft));r.post('/',requireAuth,a(c.create));r.get('/',recommendedAuth,a(c.list));r.get('/:id',a(c.getOne));module.exports=r;
